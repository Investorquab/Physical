# PHYSICAL

Real-world infrastructure, made programmable.

Scaffold generated by Claude A (lead engineer). See docs/ (populated as the build
progresses) and PHYSICAL_ARCHITECTURE_PLAN.md for the full architecture.

## Repo layout
- apps/web            Next.js frontend (Claude B owns this)
- apps/api            Backend API (Claude A)
- apps/worker-ingestion  Polls OpenAQ, submits Sepolia tx (Claude A)
- apps/worker-oracle     Attestcoin Protocol oracle worker (Claude A)
- packages/shared-types  API contract, source of truth for both frontend and backend
- packages/db            Prisma schema

## Setup
See root .env.example — copy to .env and fill in secrets. Never commit .env.
